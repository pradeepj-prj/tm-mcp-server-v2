# MCP server host/port (CF sets $PORT automatically)
HOST=0.0.0.0
PORT=8080

# TM Skills API base URL (no trailing slash)
TM_API_BASE_URL=http://localhost:8000

# API key for authentication (leave empty if API auth is disabled)
TM_API_KEY=

# HTTP request timeout in seconds
TM_API_TIMEOUT=30.0

# Audit log SQLite database path (ephemeral on CF, resets on redeploy)
AUDIT_DB_PATH=audit.db
