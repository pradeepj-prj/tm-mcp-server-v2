---
applications:
  - name: tm-skills-mcp-v2
    memory: 256M
    disk_quota: 512M
    instances: 1
    buildpacks:
      - python_buildpack
    command: null
    health-check-type: port
    timeout: 120
    env:
      TM_API_BASE_URL: "https://tm-skills-api.cfapps.ap10.hana.ondemand.com"
      # TM_API_KEY: set via `cf set-env` — never commit secrets
